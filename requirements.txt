# ============================================================
# embedded-device-agent 运行时 + 开发/测试依赖
# 版本已 pin 到 .venv 实际解析结果（Python 3.13.13，pip freeze）。
# ============================================================

# ---- 运行时：编排与 LLM ----
langgraph==1.2.8
langchain-core==1.4.8
langchain-anthropic==1.4.8
langchain-openai==1.3.3
# 说明：langchain-openai 同时提供 OpenAIEmbeddings，
# 可满足 design.md 中 LangGraph Store 语义召回所需的 embeddings，
# 因此无需额外引入独立 embeddings 包（LangGraph Store 由 langgraph 内置）。

# ---- 运行时：数据契约与配置 ----
pydantic==2.13.4
pydantic-settings==2.14.2
pyyaml==6.0.3

# ---- 运行时：设备/串口 ----
pyserial==3.5

# ---- 运行时：API 边界 ----
fastapi==0.139.0
uvicorn[standard]==0.50.2
python-dotenv==1.2.2

# ---- 开发/测试 ----
pytest==9.1.1
pytest-asyncio==1.4.0
hypothesis==6.156.2
mypy==2.2.0
